<nav id="nav" class="nav flex">

    <ul class="menu flex flex-col md:flex-row items-center list-reset text-base">
        {% for item in navigation.main %}
        <li class="level-1 mb-4 md:mr-4 md:mb-0">
            <a href="{{ item.url | safe }}" class="link font-bold {% if page.url == item.url %}active text-pink-dark{% endif %}
            ">{{ item.label }}</a>
        </li>
        {% endfor %}
        <li>
            <a class="inline-block btn rounded md:ml-2 py-3 px-6 text-white bg-nd-gradient hover:bg-nd-gradient-reverse font-bold" href="#" target="_blank" rel="noopener">Contact</a>
        </li>
    </ul>

</nav>